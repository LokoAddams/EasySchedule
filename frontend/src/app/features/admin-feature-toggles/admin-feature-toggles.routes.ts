import { Routes } from '@angular/router';

import { AdminFeatureToggles } from './admin-feature-toggles';

export const ADMIN_FEATURE_TOGGLES_ROUTES: Routes = [
  { path: '', component: AdminFeatureToggles },
];
